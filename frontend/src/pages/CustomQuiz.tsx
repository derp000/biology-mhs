import Quiz from "../components/Quiz";

interface CustomQuizProps {
  reviewChapters: number;
}

const CustomQuiz = ({ reivewChapters }: CustomQuizProps) => {
  
  // return <Quiz questions={} />;
};

export default CustomQuiz;
